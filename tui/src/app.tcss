#sidebar {
    width: 35%;
}

ProfileList, ProcessList {
    height: 1fr;
    border: round $primary;
    padding: 0 1;
}

#main {
    width: 1fr;
}

DetailPanel {
    height: auto;
    min-height: 9;
    border: round $primary;
    padding: 0 1;
}

LogPanel {
    height: 1fr;
    border: round $primary;
    padding: 0 1;
}
